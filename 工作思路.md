# 钛酸钡（BaTiO₃）掺杂晶体 · 结构→介电预测 工作思路

> 目标：以 BaTiO₃ 为基，用机器学习预测/筛选 A/B 位掺杂晶体的介电（及铁电代理）性质。
> 文档状态：v1.6（§4.2.4 补"权重的文献依据与升级路线"：无权威权重表的说明、Kendall CVPR 2018 / GradNorm / PCGrad 出处、v2 不确定度加权代码与两个实现坑。v1.5：§4.2.4 补头结构明细/容量选项/损失权重依据；新增 §4.2.9 训练诊断与容量校验（欠/过拟合判读、小子集拟合测试、三个校验实验）。v1.4：§4.2.4 定稿四头设计（metal 可选开关、gap 掩码 gap>0 派生、带边双输出、统一标签层）。v1.3：新增 §4.2 图模型构建思路；原 §4.2/§4.3 顺延为 §4.3/§4.4。v1.2：§4.1.1 升级为"已实现的图数据 Schema"：晶体图/线图的节点与边参数、标签表、分片与加载方式）

---

## 0. 一句话路线

**先解决"结构 → 介电"的数据问题**：母体 BaTiO₃ 扩超胞 → 按位点掺杂 → 生成有序近似结构 → 用 DFPT / 文献打介电标签 → 训练模型 → 预测排序掺杂方案 → 主动学习闭环补点。

核心矛盾：**结构可以任意生成，介电标签极其稀缺。**

---

## 1. 数据现状（已实测，2026-09）

用严格判据"以 BaTiO₃ 为基"筛选：ABO₃ 化学计量（A 位总数 = B 位总数，O = 3×A），且 Ba 占 A 位 ≥ 50%、Ti 占 B 位 ≥ 50%。

| 数据源 | 口径 | 数量 | 说明 |
|---|---|---|---|
| MP | 含 Ba+Ti+O | 353 | 含大量非 BaTiO₃ 相（硅酸盐/磷酸盐等） |
| MP | **严格 BaTiO₃ 基** | **35** | 母体多型 + 掺杂有序相 |
| MP | 严格 BaTiO₃ 基且含取代元素 | **24** | 如 BaSrTi₂O₆、Ba₂SrTi₃O₉、Ba₄Ti₅PbO₁₅、BaZrTiPbO₆、BaLaTiFeO₆ |
| MP | dielectric 全库 | 7332 | 结构 + e_total/e_ionic/e_electronic/n |
| MP | **有介电标签的 BaTiO₃ 基** | **3** | 全是纯 BaTiO₃ 多型：mp-5986 / mp-5020 / mp-5777 |
| MP | 有介电标签的**掺杂** BaTiO₃ | **0** | 无 |
| AFLOW (ICSD) | Ba-Ti-O 三元 | 64（53 钙钛矿型） | BaTiO₃ 各多型实验结构 |
| AFLOW (ICSD) | Ba-Ti-O-X 四元 | 36（**0** 钙钛矿型） | 无可用的掺杂 BaTiO₃ 钙钛矿相 |
| ColabFit | 全库 | 504 个数据集 | "solid solution"/"SQS" 命中 0；含 BaTiO₃ 1；**无介电标签** |
| Zenodo | dataset 记录（关键词） | solid solution 35、SQS 2、doped BaTiO₃ 1 | 零散论文附件，非系统数据库 |

**结论：掺杂 BaTiO₃ 的介电标签必须自建。** MP 的 24 条掺杂有序相只有结构，只能当参照/验证。

---

## 2. 两个必须分开的"介电"定义

| | MP DFPT 数据 | 实验数据 |
|---|---|---|
| 泛函/方法 | PBE + DFPT | 测量（频率、温度、电场） |
| 相 | 多为高温对称相（立方） | 室温铁电相（四方） |
| BaTiO₃ 值 | e_total ≈ 11.55（e_ionic 5.63，e_electronic 5.92） | ε_r 可达 10³ 量级 |
| 用途 | 训练"结构→DFT 介电" | 工程介电性质 |

**规则：两种标签不能混在同一回归目标里。** 建议主目标用 `e_ionic`（离子贡献，与铁电软模最相关），实验数据单独作为高保真验证/多保真微调。

---

## 3. 第一阶段：结构 → 介电 数据集构建

### 3.1 母体结构

- 首选 MP 的 BaTiO₃ 相：立方 mp-5986（5 原子胞，便于扩超胞）。
- 若研究室温铁电相，取四方相（c/a > 1）作母体，并在文档中记录所选相。

### 3.2 掺杂空间定义（化学空间）

| 位点 | 宿主 | 常见掺杂元素 | 类型 |
|---|---|---|---|
| A 位 | Ba²⁺ | Sr²⁺、Ca²⁺、Pb²⁺（等价）；La³⁺、Bi³⁺、K⁺、Na⁺（异价） | 等价/施主-受主 |
| B 位 | Ti⁴⁺ | Zr⁴⁺、Sn⁴⁺、Hf⁴⁺（等价）；Nb⁵⁺、Fe³⁺、Mn³⁺（异价） | 等价/施主-受主 |

- 浓度 x：由超胞大小决定。2×2×2 → 步长 12.5%；3×3×3 → 3.7%。
- 电荷补偿规则（异价掺杂必须处理）：
  - 施主 Nb⁵⁺→Ti⁴⁺（净 +1）→ **Ba 空位**补偿（每 2 个 Nb 去掉 1 个 Ba）；
  - 受主 Fe³⁺→Ti⁴⁺（净 −1）→ **氧空位**补偿（每 2 个 Fe 去掉 1 个 O）；
  - 记录 `charge_imbalance` 与 `n_vacancies` 作为元数据。

### 3.3 结构生成

```te
BaTiO3 母体
  → 扩超胞 (2×2×2 = 40 原子)
  → 选定掺杂位点数 (n_A, n_B) 与元素
  → 取代位点排列
      · 小超胞：pymatgen 枚举全部排布 + 对称性去重（StructureMatcher）
      · 大超胞/SQS：随机采样或 sqsgenerator 生成准随机结构
  → 可选电荷补偿（去 Ba / 去 O）
  → 输出 CIF / pymatgen.Structure（带 metadata）
```

示例：BaSr₇Ti₈O₂₄ = (Ba₀.₁₂₅Sr₀.₈₇₅)TiO₃ 的有序超胞（MP 中可查到对应有序相 mp-1099778，可作为对照/校验）。

### 3.4 标签获取（三档并行）

| 档 | 方法 | 产出标签 | 数量 | 成本 |
|---|---|---|---|---|
| T1 低保真 | 通用 ML 势（CHGNet / MACE-MP-0 / M3GNet）弛豫 | 能量、形成能、c/a、Ti 偏心位移、八面体畸变 | 10³–10⁴ | 低（GPU 分钟级） |
| T2 高保真 | DFT：DFPT 介电 + Berry 相位极化 | e_total、e_ionic、e_electronic、结构 | 30–200 | 高（每点小时级） |
| T3 实验 | 文献汇编（Landolt-Börnstein、Springer Materials、JACS/JECS/IEEE UFFC） | ε_r、tanδ、Tc、P_r（含测量条件） | 30–200 | 中（人工） |

### 3.5 数据 Schema（统一存储）

```text
表 records.csv
  record_id, source(MP/DFPT/MLIP/EXP), material_id|citation,
  composition, formula, a_site_dopants, b_site_dopants, x_A, x_B,
  structure_ref (cif/pkl 路径),
  e_total, e_ionic, e_electronic,            # T2
  energy_per_atom, formation_energy, e_above_hull,  # T1/MP
  c_over_a, octa_disp_mean, octa_disp_max, cn_mean, # 结构代理
  epsilon_r, tan_delta, Tc, Pr, T_meas, freq,  # T3（必须带条件）
  charge_imbalance, n_vacancies,
  split_group (按组成/体系分组)
```

> 关键：**T3 必须记录测量温度/频率**，否则同成分数据相互矛盾。

---

## 4. 第二阶段：特征与模型

### 4.1 特征

- 成分：元素分数 + 元素属性统计（电负性、离子半径、原子质量、族/周期）。
- 结构：晶格参数、体积、密度、空间群、最近邻距离统计、配位数、**c/a 四方性、Ti 偏心位移、氧八面体畸变**（铁电代理量）。
- 掺杂：浓度 x_A/x_B、掺杂元素身份、电荷不平衡、空位浓度。

#### 4.1.1 图数据 Schema（已实现，v1.2；供 §4.2 的 GNN 与迁移模型直接使用）

全量图数据已建好（脚本：`data_prep/build_graphs.py` 单数据集、`data_prep/build_all_graphs.py` 批量；构建器 `GraphBuilder`）。

**A. 晶体图**（`graph/<数据集>/crystal_graph_partNNNNN.pkl`；对象 `graph_schema.CrystalData`，每分片 `{material_id: Data}`）

*节点 = 原子*（N = 晶胞原子数）

| 参数 | 形状 / 类型 | 内容 |
|---|---|---|
| `x` | [N, 149] float32 | 节点特征向量（构成见下表） |
| `z` | [N] int64 | 原子序数（备用，便于恢复元素身份） |

`x` 的 149 维布局：

| 区间 | 维度 | 内容 | 标准化 |
|---|---|---|---|
| [0:118) | 118 | 元素 one-hot（按原子序数 Z−1 索引的固定 118 维表） | 否 |
| [118:125) | 7 | 周期 one-hot（第 1–7 周期） | 否 |
| [125:144) | 19 | 族 one-hot（第 1–18 族；第 19 维 = 镧系/锕系/未知） | 否 |
| [144] | 1 | 电负性（Pauling 标尺） | 是 |
| [145] | 1 | 平均离子半径 / Å | 是 |
| [146] | 1 | 价电子数（最外层 s/p + 次外层 d + 次次外层 f） | 是 |
| [147] | 1 | 氧化态（BVA 键价法逐位点推断，标量） | 是 |
| [148] | 1 | 氧化态有效掩码（0/1，BVA 失败为 0） | 否 |

标量列 [144:148] 用 `graph/feature_stats.json`（预训练集 dielectric 统计的全局 mean/std）标准化、缺失位点归零；两阶段共用同一套统计量，不在单结构内标准化。

*边 = 8 Å 截断内近邻原子对，每个原子最多保留最近 12 个*（周期性搜索、以每个原子为中心；原点选在哪儿不影响——距离与夹角都是平移不变）。

| 参数 | 形状 / 类型 | 内容 |
|---|---|---|
| `edge_index` | [2, E] int64 | 第 0 行 = 邻居，第 1 行 = 中心原子；消息流方向"邻居 → 中心" |
| `edge_attr` | [E, 48] float32 | 原子间距离的高斯 RBF：48 个中心等距铺在 0–8 Å（间距 0.170 Å，σ = 间距） |

**B. 线图**（`graph/<数据集>/line_graph_partNNNNN.pkl`；结构 `{material_id: {line_edge_index, line_edge_attr, n_edges}}`）

- **线图节点 = 晶体图的边**（编号 0..E−1）；节点特征不单独存储，直接复用对应边的 `edge_attr`（48 维距离 RBF）。
- **线图边 = 共享同一中心原子（同一 dst）的两条键**（ALIGNN 口径），特征为两者夹角。

| 参数 | 形状 / 类型 | 内容 |
|---|---|---|
| `line_edge_index` | [2, L] int64 | 取值为**晶体图边编号** [0, E)；第 0 行 = 键 a、第 1 行 = 键 b（二者共享中心原子） |
| `line_edge_attr` | [L, 1] float32 | 两键夹角（度，0–180） |
| `n_edges` | int | 对应晶体图边数（用于两文件配对校验 / batch 偏移） |

规则与数量：

- 每条角双向存两遍（a→b、b→a，特征相同）；
- 同一中心原子的同一对邻居只算一次；**同一元素 index 的两个周期镜像之间不成角**（排除伪角）；
- 夹角用邻居的周期镜像坐标计算（`PeriodicNeighbor.coords`）；
- E ≈ min(12, 8 Å 邻居数)·N，L ≈ 2·Σ_c C(k_c,2)，实测 L ≈ 10·E（E 为原图边数）；
- **1 维角度不是精度妥协**：模型侧做可学习 Fourier 展开与数据侧固定 RBF 表达力相当，却省 ~100 GB 磁盘（展开写法见 §4.2.1）。

**C. 标签**

- `data.y`：主目标（每图 [1] float32）；其余任务标签同为 [1] 张量、属性名 = 列名；无该标签的图**不写该属性**；训练前由 §4.2.4 的统一标签层补齐 NaN 再拼批（PyG 拼批要求属性集一致，不要依赖 `hasattr`）。
- `data.split_group`：分组划分键（按组成，防同组成泄漏）。
- 位点级标签（仅 vacancy_screening）：`data.vacancy` [N, 1] float32 = 各原子位点的空位形成能；`index.csv` 另记 `site_vacancy_mean/min`。

| 数据集 | 图数 | 主目标 `y` | 多任务标签（附加属性） |
|---|---|---|---|
| dielectric | 7,327 | e_total | e_ionic, e_electronic |
| formation_energy_band_gap | 154,030 | formation_energy_per_atom_eV | band_gap_eV, energy_above_hull_eV_per_atom, cbm_eV, vbm_eV, efermi_eV, gap_from_edges_eV |
| ferroelectric | 2,408 | polarization_uC_cm2 | bandgap_polar_eV, energy_diff_eV_per_atom |
| batio3 | 153 | formation_energy_per_atom_eV | band_gap_eV, energy_above_hull_eV_per_atom, e_total, e_ionic, e_electronic, n, homogeneous_poisson, debye_temperature, universal_anisotropy, num_magnetic_sites, energy_per_atom, decomposition_enthalpy, equilibrium_reaction_energy_per_atom |
| batio3_doped | 252 | formation_energy_per_atom | energy_above_hull |
| vacancy_screening | 153,234 | formation_energy_per_atom | energy_per_atom, E_mace, ehull, mp_gap, stable, theoretical ＋ 位点级 vacancy [N,1] |
| 2d_layers | 8,017 | Formation_energy | E_MACE, HEE, LNE, LDFE, ehull_MP, factor_2D, final_factor_2D |

已建图数据规模（2026-09-22）：**8 个数据集 / 325,421 图 / 62.57 GB**（晶体图 34.45 GB + 线图 28.11 GB）。每个数据集目录含 `crystal_graph_part*.pkl`、`line_graph_part*.pkl`、`index.csv`（图清单、标签、分组）、`meta.json`（构建参数 + 特征统计）。

**D. 分片与加载**

- 2,000 图/片；crystal 与 line 分片**同编号、同 material_id 顺序**，成对加载；
- 加载前需把 `data_prep` 加入 `sys.path` 并 `from graph_schema import CrystalData`（pickle 反序列化依赖该模块）；
- **batch 偏移**：`CrystalData.__inc__` 让 `line_edge_index` 按 **num_edges** 偏移；PyG 默认按 num_nodes 偏移，不修正会把线图边指到错误的原图边上（`graph_schema.py` 已处理）；
- 内存：单对分片（晶体+线图）约 300 MB；全库驻留约 89 GB → 训练必须按分片流式加载。

与 v1.1 规划口径的差异：元素编码由"64 维可学习嵌入"改为"118 维 one-hot"（等价于嵌入的前一层，且跨阶段无需词表对齐）；距离 RBF 由 41 维（0.2 Å 步长）改为 48 维（0.17 Å）；键角由"RBF 37–41 维"改为"1 维角度 + 模型侧展开"。

设计约束（硬性，沿用）：

- 两阶段（预训练/微调）共用同一构建器与同一套归一化统计量（`graph/feature_stats.json`），否则微调输入分布漂移。
- 元素覆盖检查：预训练元素 ⊇ 微调元素（固定 118 维 one-hot 表，天然对齐）。
- 键角必须通过线图表达；不要往消息传递里塞手写物理权重。
- 消融实验：纯 one-hot / 纯物理属性 / 融合三档，在 BaTiO₃ 族留出集上对比（评估协议见 §4.4）。

### 4.2 图模型构建思路（v1.3 新增；主方案已定稿）

主公式：**一个共享骨干 + 多任务输出头 + 掩码多任务损失 + 两阶段迁移**。数据由 §4.1.1 已建好的晶体图 / 线图直接喂入。

```text
晶体图（N 原子）+ 线图（E 键，L 角）
  → ① 输入编码：节点 Linear(149→64)；键 MLP(48→64)；角 Fourier(1→d_a)
  → ② 消息传递 × 4（A 块 = 晶体图卷积 → 线图卷积，交替进行）
  → ③ 池化：mean ‖ max（定长 128 维）
  → ④ 多任务头：3 个图级头 + 1 个节点级头（带边头双输出；metal 头可选、默认关闭）
  → ⑤ 损失：SmoothL1（+ 可选 BCE）+ 缺失标签掩码 + 任务权重
```

三个"为什么"：

- **为什么共享骨干（而不是每目标一个独立模型）**：小样本下多任务 = 正则化，共享表示吃全部数据的梯度；每目标独立模型只在消融里作为对照（§4.2.7）。
- **为什么双图**：晶体图管配位与键长；线图管键角——O–Ti–O 偏离 180° 是四方畸变的核心信号。ALIGNN 实证：加键角才能区分 CGCNN 分不开的高度相似结构。
- **为什么要池化**：标签是"一个晶体一个数"，消息传递输出是 (N, d) 变长张量；池化把它变成定长向量，同时保证置换不变（mean = 平均环境；max = 最极端位点）。

#### 4.2.1 输入编码

| 输入 | 数据侧（§4.1.1） | 模型侧 |
|---|---|---|
| 节点 | `x` [N, 149]（118 one-hot + 7 周期 + 19 族 + 4 标量） | `Linear(149→64) + SiLU + LayerNorm`（one-hot 经线性层等价于嵌入，跨阶段无需词表对齐） |
| 键 | `edge_attr` [E, 48]（距离 RBF） | `MLP(48→64→64)` |
| 角 | `line_edge_attr` [L, 1]（角度，度） | 可学习 Fourier 展开 → `MLP(2K+1→d_a)`，默认 K=8、d_a=32 |

角度 Fourier 展开（补 §4.1.1 的"见 §4.2 备注"；与固定 RBF 表达力相当，省 ~100 GB 磁盘）：

```python
class AngleFourier(nn.Module):
    def __init__(self, k_max=8, out_dim=32):
        super().__init__()
        self.register_buffer("k", torch.arange(1.0, k_max + 1.0).view(1, -1))
        self.net = nn.Sequential(nn.Linear(2 * k_max + 1, out_dim), nn.SiLU(),
                                 nn.Linear(out_dim, out_dim))

    def forward(self, deg):
        rad = deg * math.pi / 180.0
        feats = torch.cat([deg / 180.0, torch.sin(rad * self.k), torch.cos(rad * self.k)], dim=-1)
        return self.net(feats)
```

#### 4.2.2 消息传递（4 层；A 块交替）

每个 A 块 = 先晶体图卷积、后线图卷积（ALIGNN 口径）。

晶体图卷积（消息方向与 `edge_index` 一致：邻居 → 中心）：

```text
m_{j→i} = σ(W_f·[h_j ‖ h_i ‖ e_ji]) ⊙ softplus(W_s·[h_j ‖ h_i ‖ e_ji])
h_i     ← LayerNorm(h_i + softplus(W_o·[h_i ‖ Σ_j m_{j→i}]))
```

线图卷积（键 → 键，经共享中心原子的夹角）：

```text
m_{b→a} = MLP([e_a ‖ e_b ‖ φ(θ_ab)])
e_a     ← LayerNorm(e_a + mean_b m_{b→a})
```

- 门控 `σ(·)` 是网络自己学的"键重要性"，**不手写物理权重**（§4.1.1 硬约束）。
- 默认：K = 4；节点/键宽 64；卷积隐层 128–256（全模型约 4–6×10⁵ 参数）。
- 4 层的依据：每层感受野 ≈ 一个键长（~2 Å），4 层 ≈ 8 Å 覆盖第二配位壳；更深会过平滑。

#### 4.2.3 池化（定稿：mean + max）

- `global_mean_pool(x, batch) ‖ global_max_pool(x, batch)`：把变长 (N, 64) 压成定长 (128,)。
- mean = 平均化学环境（成分主导量，如形成能）；max = 最极端局部环境（最畸变位点，对带隙/缺陷能关键）。**不用 sum**：目标是每原子/强度量，sum 会随原子数缩放。
- 弃案记录：**"取某个原子向量当图向量"不成立**——索引无物理含义（原子顺序置换后结果改变，违反对称性），且单向量存在信息瓶颈（over-squashing）。若想让消息传递直接产出图向量，正确形式是虚拟全局节点（暂不采用）。
- 全局特征（可选增强，当前 schema 未含）：晶格 a/b/c、α/β/γ、体积/原子、密度（8 维）拼在池化向量后；启用需在构建器补 `data.global_feat` 并保持两阶段一致。
- **位点级标签不走池化**：`vacancy_screening` 的 `data.vacancy` [N, 1] 是节点级任务，直接在 h 上接回归头；只有图级标签才池化。

#### 4.2.4 多任务头与损失（v1.5 定稿）

当前训练目标四类：**带隙、带边（CBM/VBM）、缺陷形成能、形成能**（介电/铁电数据仅作背景核对，不在训练目标内）。输出头 = 3 个图级头 + 1 个节点级头：

| 头 | 目标 | 层级 | 标签来源 | 损失 | 掩码 |
|---|---|---|---|---|---|
| `formation` | 形成能 | 图级 | formation_energy_band_gap / batio3 / batio3_doped（vacancy_screening 待审计） | SmoothL1(β=0.05) | 无 |
| `gap` | 带隙 | 图级 | formation_energy_band_gap / batio3 | SmoothL1(β=0.1) | **由 `gap > 0` 派生**（金属行自动排除，无需金属识别） |
| `edges` | 带边位置（cbm/vbm 双输出） | 图级 | formation_energy_band_gap（`cbm_eV` / `vbm_eV`） | SmoothL1(β=0.1) ×2 | 缺失置 NaN |
| `vacancy` | 缺陷（空位）形成能 | **节点级** | vacancy_screening 位点标签 [N,1] | SmoothL1(β=0.1) | 位点级 NaN |

**头结构明细（默认模板，图级头同构）**：

| 头 | 输入 | 结构 | 输出 | 参数量 |
|---|---|---|---|---|
| `formation` / `gap` | pooled [136] | Linear(136→128)-SiLU-Dropout-Linear(128→64)-SiLU-Linear(64→1) | 1 | ≈ 2.6 万 |
| `edges` | pooled [136] | 同上，末层 `Linear(64→2)`（cbm/vbm 共享躯干） | 2 | ≈ 2.6 万 |
| `vacancy` | **h [N,64]**（原子表示，不走池化） | Linear(64→64)-SiLU-Linear(64→1) | N×1 | ≈ 0.4 万 |
| `metal`（可选开关） | pooled [136] | 同 `formation`（输出 logit） | 1 | ≈ 2.6 万 |

- 四头合计 ≈ 10.8 万参数（约占全模型 20%）；"同构"是默认假设，其可靠性由 §4.2.9 的容量扫描验证，不预设永不欠/过拟合。
- 容量选项（仅在诊断确认需要时启用）：瘦身 A `136→64→32→1`（≈1.1 万/头）／单隐层 `136→64→1`（≈0.9 万/头）／共享躯干（合计 ≈ 2.1 万）／线性头（探针基线，137 参数）。
- 细节决策：输出层不加激活（回归线性、分类出 logit）；输出 bias 用**掩码后子集**的标签均值初始化（gap 头尤其重要）；dropout 放隐层后（预训练 0–0.1、微调 0.1–0.3）；可选在 pooled 后加一层共享 LayerNorm(136)。
- **统一标签层（必须先做）**：建图时缺失标签不写属性（`build_graphs.py:298-301`），PyG 拼批以首个样本的键为准（`collate.py:90/95`），直接混批会 KeyError 或静默丢标签。训练前每个图 materialize `data.labels [1,4]`（formation / gap / cbm / vbm，缺失 NaN）+ `data.vacancy [N,1]`（缺失 NaN）；拼批后为 `[B,4]` 与 `[ΣN,1]`。
- 一致性项（可选）：`gap ≈ cbm − vbm`，三值齐备时生效；权重 0.1，建议 **ramp-up**（前 5–10% 步数从 0 升到 0.1，避免早期两头噪声互相拉扯）。
- **可选开关：`metal` 分类头（默认关闭）**：本阶段不做金属识别；代码保留开关（`use_metal=False`），目标由 `gap == 0` 派生——不占标签槽位、不需要重建数据。日后启用：开启后从头训，或从现有 checkpoint 加载骨干（`strict=False`）并冻结、只训该头。
- 缺陷能若引入多来源（MLIP / DFT），必须各挂独立头，数值不可比。

**损失权重依据（v1.5 补充）**：

| 头 | 权重 | 依据 |
|---|---|---|
| `formation` | 1.0 | 预训练主力目标、数据最大（15.4 万），骨干表示学习的主梯度 |
| `gap` | 1.0 | 第二主力目标；与带边共享电子结构信息 |
| `cbm` / `vbm` | 0.5 + 0.5 | 有效覆盖约 57%；与 gap 信息冗余；绝对位置难度高，权重过大会把骨干拉向噪声；一致性项补强 |
| `vacancy` | 1.0 | 四目标之一、缺陷唯一来源；节点损失**必须用 mean 而非 sum**（否则以 N 倍梯度压过图级任务） |
| `metal`（可选） | 0.2 | 辅助开关任务，低权重避免干扰主任务 |

- 权重在**标准化空间**定义：每头先 z-score（预训练集统计、剔除家族、两阶段共用），评估回算 eV；SmoothL1 的 β 也按标准化单位理解（0.05–0.1 ≈ 接近 L1 的鲁棒回归）。
- **采样与权重的分工**：采样比例 `∝ 1/sqrt(N_t)` 决定批构成，权重决定批内各项贡献；先固定采样，再调权重。
- **监控与校准**：逐项打印 `w_t × L_t`（单项占比 > 70% 先调采样/降权）；主任务权重只做 ±2 倍消融；不在权重上做大规模网格搜索。
- **动态加权（v2 备选，非第一版）**：不确定性加权（Kendall et al., CVPR 2018, arXiv:1705.07115）`Σ (1/2σ_t²)·L_t + log σ_t`；batch 内无该任务标签时该项**跳过**（传 0 会把 σ 推散）、σ 建议 clamp；梯度冲突用 GradNorm（Chen et al., ICML 2018）/ PCGrad（Yu et al., NeurIPS 2020）。第一版用固定权重 + 一致性 ramp-up 足够。

**权重的文献依据与升级路线（v1.6 补充）**：

- 前提：**不存在通用的"权威权重表"**——权重取决于标签噪声与尺度、数据量、任务相关性与采样比例。已读三篇文献均不涉及多任务联合权重：ALIGNN-CSP 的回归与可合成性分类分开训练；MatterGPT 是条件生成的 token 交叉熵；GA 论文以 DFT 能量作适应度。故权重依据 = 多任务学习方法学 + 本地验证，不照抄文献。
- 权威方法（按推荐顺序）：
  1. **不确定度加权（首选）**：Kendall, Gal & Cipolla, CVPR 2018（arXiv:1705.07115）。把各任务残差视为方差 σ_t² 的高斯噪声，最大化联合似然等价于 `L = Σ_t [ 1/(2σ_t²)·L_t + log σ_t ]`；σ_t 随训练自动学习（任务噪声大 → 权重自动变小）。
  2. **梯度范数平衡**：GradNorm（Chen et al., ICML 2018, arXiv:1711.02257）。
  3. **梯度冲突投影**：PCGrad（Yu et al., NeurIPS 2020, arXiv:2001.06782）。
  4. **工程惯例（本表 v1 权重的来源）**：各任务先标准化，再把损失缩放到梯度量级可比、按重要性分档（主力 1.0 / 派生 0.5 / 辅助 0.2）——也是原子势模型（能量/力/应力多目标）的通行做法。
- v2 实现（切换时直接可用）：

```python
class UncertaintyWeights(nn.Module):
    def __init__(self, tasks):
        super().__init__()
        self.log_var = nn.ParameterDict({t: nn.Parameter(torch.zeros(())) for t in tasks})

    def forward(self, losses):
        total = None
        for t, l in losses.items():
            if l is None:
                continue
            lv = self.log_var[t].clamp(-4.0, 4.0)
            term = 0.5 * torch.exp(-lv) * l + 0.5 * lv
            total = term if total is None else total + term
        return total
```

- 两个实现坑：① batch 内无该任务标签时把该项置 `None` 跳过——传 `0.0` 会让对应 `log σ` 被推向 −∞、权重发散；② `log σ` 加 clamp（如 ±4）。切到 v2 后，把学到的 σ_t 记录为任务噪声诊断，与 v1 指标对照。
- 升级路线：v1 固定权重（当前）→ v2 Kendall 不确定度加权（训练稳定后）→ v3 仅当出现负迁移时上 GradNorm/PCGrad。任何版本，指标均按任务分开报告，不得用权重掩盖弱任务。

#### 4.2.5 两阶段迁移与冻结策略

- 阶段一：全库预训练（多任务、掩码）；阶段二：BaTiO₃ 族（batio3 / batio3_doped / vacancy 子集）微调。
- 冻结配方：先全冻骨干只训头（线性探针基线）→ 再解冻 top-1~2 个 A 块，lr 降 10–100 倍。
- 参考实证：ALIGNN-CSP 冻结除最后两层外全部层，误差 −27.2%。
- **必修对照**：微调 / 线性探针 / 从零训练（family-only）＋ 描述符基线（GPR/RF，见 §4.3）。

#### 4.2.6 默认超参

| 超参 | 预训练 | 微调 |
|---|---|---|
| K / 节点宽 / 卷积隐层 | 4 / 64 / 128–256 | 同左（冻结前半） |
| 头结构 | 2–3 层 MLP | 同 |
| dropout | 0–0.1 | 0.1–0.3 |
| batch | 32–128 | 16–32 |
| 优化器 | AdamW（wd 1e-5~1e-4），warmup 5% + cosine | AdamW |
| 学习率 | 3e-4 | head 1e-3；解冻块 1e-5 |
| 梯度裁剪 / 早停 | 5.0 / 是 | 5.0 / patience 50 |

#### 4.2.7 消融清单（报告用）

1. 骨干：仅晶体图 vs 晶体图 + 线图（键角增益）。
2. 池化：mean vs max vs mean + max。
3. 特征：纯 one-hot vs 纯物理标量 vs 融合（§4.1.1 已列）。
4. 训练：多任务共享 vs 每目标独立模型；微调 vs 线性探针 vs scratch。
5. 集成：同架构 5 个随机种子平均（顺带给出预测不确定度）。
6. 头容量：宽度 {64, 128, 256}（与 §4.2.9 校验实验合并执行）。

#### 4.2.8 对接已实现数据的硬约束

- 两阶段同一构建器 + 同一 `graph/feature_stats.json`；不在单结构内标准化。
- `line_edge_index` 的 batch 偏移由 `graph_schema.CrystalData.__inc__` 处理（按 num_edges），模型侧不要重复偏移。
- 角特征只在模型侧展开（数据侧 1 维），兼顾表达力与磁盘。
- 训练按分片流式加载 `CrystalData`（内存口径见 §4.1.1-D）；模型代码待建。

#### 4.2.9 训练诊断与容量校验（v1.5 新增）

**逐头学习曲线判读**（永远分头看，不看总和）：

| 症状 | 诊断 | 优先处理 |
|---|---|---|
| 训练损失也降不下去（train ≈ val，都高） | 欠拟合——多为骨干表示缺信息，而非头容量 | 先做小子集拟合测试；再考虑加宽头 / 解冻骨干 / 补特征 |
| 训练低、验证高且差距随 epoch 扩大 | 过拟合 | 提 dropout / weight decay、缩头（见 §4.2.4 容量选项）、早停、种子集成 |
| 单头退化、其余正常 | 任务冲突（骨干被其他任务抢占） | 查采样比例与分项损失占比；必要时上 GradNorm / PCGrad（v2） |

**小子集拟合测试**：取 100–200 条训练样本、关 dropout，看训练损失能否压到接近 0。能压到 → 容量无问题，走泛化/正则路线；压不到 → 才轮到加宽头、换结构、调 lr。

**三个校验实验**（同时作为报告素材，与 §4.2.7 合并汇报）：

1. 冻结骨干探针：加载预训练权重、全冻骨干只训头，与微调对比——差距大 ≡ 问题在骨干表示；
2. 头容量扫描：宽度 {64, 128, 256} 在子集上各跑一次——找验证 MAE 拐点，确认默认模板与瘦身选项的取舍；
3. 微调正则扫描：dropout {0.1, 0.3} 对比家族留出集——确认过拟合区间。

**多种子诊断规则**：5 个随机种子训练后——种子间指标差异大 ≡ 优化/初始化盆地问题（用集成收尾）；各种子一致地差 ≡ 优先怀疑数据 / 掩码 / 划分 bug，而不是优化问题。

**各头风险预估（基于当前数据实况）**：`formation` 过/欠拟合风险均低（15.4 万标签 vs 2.6 万参数）；`gap` 过拟合风险低，近金属区难属数据固有；`edges` 覆盖约 57%、绝对能量难，欠拟合时先查骨干而非加宽头；`vacancy` 位点噪声大，盯紧其梯度占比；**家族微调（约 400 样本）过拟合风险最高**——冻结骨干 + dropout 0.2–0.3 + 早停 + 集成，必要时换瘦身头。

### 4.3 模型阶梯（按数据量升级）

| 数据量 | 模型 | 备注 |
|---|---|---|
| < 50 | GPR / RF（描述符） | GPR 给不确定度，适合小样本 |
| 50–500 | RF / GBM / MLP | 交叉验证，特征重要性 |
| > 500 | CGCNN / ALIGNN 风格 GNN（输入参数见 §4.1.1） | 结构敏感；键角（线图）对四方畸变更关键 |
| 迁移 | 多保真：T1/MP 预训练 → T2/T3 微调 | 目标语义须一致 |

### 4.4 评估协议（硬性）

- **按组成/体系分组划分**，禁止随机划分（同成分泄漏会虚高指标）。
- 单独报告：全测试集、BaTiO₃ 族留出集、成分外推集。
- 指标：MAE、RMSE、R²；介电长尾 → log 变换或按量级分桶。
- 报告不确定性（GPR 方差 / 集成方差 / 直接量化）。

---

## 5. 第三阶段：筛选与搜索

1. 批量生成掺杂候选（含浓度网格 + 随机排布）。
2. 模型预测 → 排序 → 导出 Top-K 结构（CIF）。
3. 遗传算法搜索（对应课程项目 2.5）：
   - 染色体 = 各位点等位基因（0=宿主，其余=掺杂元素编号）；
   - 适应度 = 预测值 − 电荷不平衡/空位惩罚；
   - 锦标赛选择 + 均匀交叉 + 变异 + 精英保留；
   - 与随机搜索对比，输出收敛曲线。

---

## 6. 第四阶段：闭环发现（对应课程项目 4）

```text
模型预测候选
   → 选不确定度最高的 N 个
   → DFPT / 实验验证
   → 加入训练集（增量或全量重训）
   → 重复 3–5 轮
```

预算控制：例如每轮 5–10 个 DFPT 点，总预算 ≤ 50 点。

---

## 7. 里程碑

| 阶段 | 交付物 | 数据规模 |
|---|---|---|
| M0 | 数据现状 + Schema + 结构生成脚本 | 0 标签 |
| M1 | T1 数据集（ML 势标签 + 结构代理） | 10³ |
| M2 | 小样本模型（GPR/RF）+ 分组评估 | T1 全量 + T2 几十点 |
| M3 | GNN + 迁移学习 | T2 100+ 点 |
| M4 | 筛选 / GA 搜索 | 生成 10³ 候选 |
| M5 | 主动学习闭环 + 报告 | 迭代 3–5 轮 |

---

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| 掺杂 BaTiO₃ 介电标签极少 | 自建 T2/T3；先用结构代理量（c/a、偏心位移）做预训练 |
| DFT 介电 vs 实验 ε_r 语义不一致 | 目标分离；多保真建模，实验单列验证 |
| domain shift（全库氧化物 vs BaTiO₃） | 单独留出 BaTiO₃ 族测试集；报告两套指标 |
| 数据泄漏（同成分不同排布） | 按组成分组划分 |
| DFPT 成本高 | ML 势先筛，DFPT 只打高价值点 |
| 异价掺杂结构不合理 | 强制电荷补偿 + 惩罚项 + 距离合理性检查 |

---

## 9. 环境与接口备注

- Python：`E:\Material_MTL\crystal_env\Scripts\python.exe`
- 已装：pymatgen、mp-api、torch(+cu126)、torch_geometric、sklearn、pandas、spglib
- `MP_API_KEY` 已配置，`MPRester()` 可直接用
- 图数据与建图脚本（Schema 见 §4.1.1）：
  - 图数据：`specific_MT/graph/<数据集>/`（8 数据集，325,421 图 / 62.57 GB）；`graph/feature_stats.json` 为标量特征统计（两阶段共用）
  - `data_prep/build_graphs.py`：单数据集建图/调试（`--limit` 冒烟）；
  - `data_prep/build_all_graphs.py`：批量建图（三阶段进度条、`.build.lock` 防并发、`--line-mode`/`--rbf-bins` 等精度旋钮）；
  - `data_prep/graph_schema.py`：`CrystalData`（修正 `line_edge_index` 的 batch 偏移，加载时需可导入）。
- MP 关键接口：
  - `summary.search(elements=[...], fields=[...])` 取结构/能量
  - `dielectric.search(material_ids=[...], fields=[...])` 取 e_total/e_ionic/e_electronic/n（只支持 material_id 或数值范围过滤）
  - 立方 BaTiO₃：mp-5986，e_total ≈ 11.55
- 结构生成注意：`Structure * (2,2,2)` 超胞；`get_all_neighbors(r)` 考虑周期性；`replace/remove_sites` 修改结构时索引降序处理。

---

## 10. 待确认决策

1. 目标是 **DFT 介电（e_ionic/e_total）** 还是 **实验 ε_r/Tc**？（两者流程不同）
2. 母体用立方相还是四方相？
3. 掺杂浓度网格：2×2×2（12.5% 步长）还是 3×3×3（3.7%）？
4. 是否引入通用 ML 势（需额外安装 chgnet / mace-torch）？
5. T3 文献数据由谁整理、覆盖哪些掺杂体系？
